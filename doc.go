// Package migrator provides a simple, flexible DB migration runner with
// pluggable sources (dir, file, vars), SQL and hook steps, history tracking
// (SQLite/MySQL), and optional transactions.
package migrator
